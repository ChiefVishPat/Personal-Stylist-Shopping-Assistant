# Chrome Extension

## Installation
1. Go to `chrome://extensions/`
2. Enable Developer mode
3. Click "Load unpacked"
4. Select the `extension` folder

## Usage
Ensure backend is running at localhost:5000, then click extension icon on any shopping site.

